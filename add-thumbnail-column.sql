-- =====================================================
-- 記事サムネイル機能追加マイグレーション
-- =====================================================
-- このスクリプトをSupabaseのSQL Editorで実行してください
--
-- 実行手順:
-- 1. Supabaseダッシュボード (https://supabase.com) にログイン
-- 2. プロジェクトを選択
-- 3. 左メニューから「SQL Editor」を開く
-- 4. 「New query」をクリック
-- 5. このスクリプト全体をコピー＆ペースト
-- 6. 「Run」ボタンをクリック
-- =====================================================

-- articlesテーブルにthumbnail_urlカラムを追加
ALTER TABLE articles ADD COLUMN IF NOT EXISTS thumbnail_url TEXT;

-- カラムの説明を追加
COMMENT ON COLUMN articles.thumbnail_url IS '記事のサムネイル画像URL（最初にアップロードした画像を自動設定）';

-- =====================================================
-- セットアップ完了！
-- =====================================================
-- 次のステップ:
-- 1. 実行が成功したか確認（エラーがないこと）
-- 2. 左メニューの「Table Editor」でarticlesテーブルを開く
-- 3. thumbnail_urlカラムが追加されたことを確認
-- =====================================================
